const routes = [
    {href: `${import.meta.env.BASE_URL}`, label: "home"},
    {href: `${import.meta.env.BASE_URL}groups/`, label: "groups"},
    {href: `${import.meta.env.BASE_URL}meetings/`, label: "meetings"},
    {href: `${import.meta.env.BASE_URL}about/`, label: "about"},
];

export default routes;